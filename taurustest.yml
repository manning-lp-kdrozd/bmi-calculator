execution:
- scenario: simple

scenarios:
  simple:
    script: bmicalcperftest.jmx
settings:
  check-interval: 5s
  default-executor: jmeter
modules:
  jmeter:
    properties:
      jmeter.save.saveservice.output_format: xml